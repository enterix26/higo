# Data Directory

This directory stores server-persisted JSON data:
- `member-overrides.json`: Stores custom user edits (names, phone numbers, custom passwords, sales amounts, HiGoIDs, and memos) updated in real-time by members and admin.
